Note:
The topology_features_git.py script requires the GGI_tuple.txt file generated by goterm_features_git.py script as input. Locate the file in the output subdirectory found in the goterms directory and place inside the output subdiectory in the topology directory before running the script.

The following packages are required for the script:
# Networkx (pip install networkx)
# pandas
# graphrole

The takes time to complete depending on the number of query genes.

From the command prompt, run python <name of the file>.py, the final output will be located in the smae directory as the script